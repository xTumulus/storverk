# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Your real personal data — never committed, so a public fork/push can't
# leak it. content.example/ and media.example/ are the tracked, editable
# templates; `npm install` (or `npm run init-content`) seeds these from
# them the first time, and never overwrites files you've already edited.
/content/
/media/

# Build-time content pipeline output — regenerated by `npm run build`/`predev`
src/generated
.vite-ssg-temp
docker/generated

# Local environment variables / secrets
.env
.env.*
!.env.example

# AI coding assistant files
CLAUDE.md
.claude/settings.local.json
.cursorrules
.cursor/
.windsurfrules
.windsurf/
.clinerules
.clinerules/
.aider*
.continue/
AGENTS.md
.github/copilot-instructions.md

# Editor directories and files
.vscode/
.idea
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
